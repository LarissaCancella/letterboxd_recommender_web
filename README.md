# LETTERBOXD RECOMMENDER WEB

## Frontend

1. Instale as dependências:
   ```
   npm install
   ```
2. Para iniciar o frontend, execute:
   ```
   npx next build
   npx next start
   ```
3. O frontend estará disponível em `http://localhost:3000`